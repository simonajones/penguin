/*
 * View model for the whole application.
 * 
 * This module simply aggregates all the page view models together. 
 */
define(["model/page", "model/queueCreate", "model/queuesView", "model/queueView"], function(page) {

	return page;
	
});
